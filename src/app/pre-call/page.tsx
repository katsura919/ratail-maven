import { PreCallForm } from "@/components/ui/pre-call-form";

export const metadata = {
  title: "Pre-Call Business Template | RETAILMavens",
  description: "Complete your business profile before your coaching call.",
};

export default function PreCallPage() {
  return <PreCallForm />;
}
